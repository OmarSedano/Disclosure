'use strict';

angular.module('disclosureApp')
    .factory('authenticationService', ['$scope','$q',
        function ($scope, $q) {
            return {

                login : function(loginModel){

                }

            };
           
        }
    ]);